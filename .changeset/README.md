# Changesets

This directory contains `.md` files that represent changes to the codebase.

When you are ready to document a change for the changelog, run `npm run changeset` and follow the prompts. These files will be automatically consumed when `npm run version-packages` is run.
